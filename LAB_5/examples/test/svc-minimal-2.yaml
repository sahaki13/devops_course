apiVersion: v1
kind: Service
metadata:
  name: gcr-svc
spec:
  type: NodePort
  selector:
    app: echo-server
  ports:
    - protocol: TCP
      port: 3000
      nodePort: 31000 # The range of valid ports is 30000-32767
