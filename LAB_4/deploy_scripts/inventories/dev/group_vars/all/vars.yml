# !! общие параметры (спец. директория all), которые применяются ко всем хостам !!
# также можно поместить в спец. файл group_vars/all.yml
ansible_host_key_checking: false
ansible_ssh_extra_args: '-o ForwardAgent=yes'
ansible_ssh_private_key_file: ${HOME}/.ssh/id_ed25519
ansible_user: debian
