# global: &global
app:
  imagePullSecrets:
    - name: gitlab-insecure-registry
  deploymentAnnotations:
    argocd.argoproj.io/sync-wave: "1"
  envs:
    DB_CONFIG_PATH: "PREPROD"
    DNS: "PREPROD"
    SVCS: "PREPROD"
    ENV: "PREPROD"
  probes:
    liveness:
      enabled: true
      path: /healthcheck
      periodSeconds: 10

serviceAccount:
  annotations:
    argocd.argoproj.io/sync-wave: "-1"

vault:
  vso:
    annotations:
      argocd.argoproj.io/sync-wave: "0"

